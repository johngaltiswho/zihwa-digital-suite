import React from "react";

/* Mock Header */
export const Header = () => (
  <header role="banner">Mock Header</header>
);

/* Mock Footer */
export const Footer = () => (
  <footer role="contentinfo">Mock Footer</footer>
);

/* ✅ Mock HeroSlider (THIS WAS MISSING / WRONG) */
export const HeroSlider = () => (
  <section data-testid="hero-slider">Mock Hero Slider</section>
);
