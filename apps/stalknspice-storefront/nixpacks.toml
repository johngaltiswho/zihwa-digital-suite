[variables]
NIXPACKS_NO_MUSL = '1'

[phases.setup]
nixPkgs = ['nodejs_20', 'pnpm-9_x']

[phases.install]
cmds = ['pnpm install --frozen-lockfile']

[phases.build]
cmds = ['pnpm --filter stalknspice-storefront build']

[start]
cmd = 'cd apps/stalknspice-storefront && pnpm exec next start'
